apiVersion: v1
kind: Service
metadata:
  name: {{ include "sensen-consumer.fullname" . }}
  labels:
    {{- include "sensen-consumer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sensen-consumer.selectorLabels" . | nindent 4 }}
